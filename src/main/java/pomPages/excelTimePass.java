package pomPages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.PageFactory;

public class excelTimePass {
	public excelTimePass(WebDriver driver) {
		PageFactory.initElements(driver,this);	
		}
}
